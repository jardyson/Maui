namespace GlobalDeclar
{
    /// <summary>
    /// 设备访问读写的值类型
    /// </summary>
    public enum DeviceReadWriteValueType
    {
        UshortType, IntType, StringType, BoolType, ByteType, FloatType, DoubleType, DateTimeType
    }
}
