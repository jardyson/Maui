<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp2.MainPage">

    <ContentPage.Resources>
        <Style TargetType="Label">
            <Setter Property="WidthRequest" Value="40"/>
            <Setter Property="HeightRequest" Value="40"/>
        </Style>
    </ContentPage.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>

        <Label Background="Orange" Grid.Row="1" Grid.Column="2" Text="{Binding model.value}"/>
        <Label Background="Green" Grid.Row="2" Grid.Column="1"/>

        <RadioButton Content="男">
            <RadioButton.ControlTemplate>
                <ControlTemplate>
                    <Grid>
                        <Rectangle Fill="Red" WidthRequest="30" HeightRequest="30"/>
                        <Label Text="{TemplateBinding Content}" VerticalTextAlignment="Center" HorizontalTextAlignment="Center"/>
                    </Grid>
                </ControlTemplate>
            </RadioButton.ControlTemplate>
        </RadioButton>
        <Button Text="button" Grid.Column="0" Command="{Binding ButtonCommand}"/>
    </Grid>
</ContentPage>
